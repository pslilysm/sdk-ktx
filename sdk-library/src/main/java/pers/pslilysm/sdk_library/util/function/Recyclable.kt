package pers.pslilysm.sdk_library.util.function

/**
 * A `Recyclable` is a object can be recycled.
 * The recycle method is invoked to release the object
 *
 * @author pslilysm
 * @since 1.0.0
 */
interface Recyclable {
    fun recycle()
}